{% extends 'AdminBundle:layouts:main.html.twig' %}

{% block content %}
    <a class="btn btn-danger pull-right" href="{{ path('admin_blanks_list', {slug: room.slug}) }}">Cancel</a>
    <h1 class="page-header">Edit Time</h1>
    {{ include('AdminBundle:Blank:_form.html.twig') }}
{% endblock %}
