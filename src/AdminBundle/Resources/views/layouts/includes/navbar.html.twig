<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/" target="_blank">EscapeRooms</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <div class="hidden-lg hidden-md hidden-sm">
                <ul class="nav navbar-nav navbar-right">
                    <li {% if ('rooms' in app.request.get('_route')) or ('blank' in app.request.get('_route')) %}class="active"{% endif %}>
                        <a href="{{ path('admin_rooms_list') }}">Rooms</a></li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li {% if 'cities' in app.request.get('_route') %}class="active"{% endif %}>
                            <a href="{{ path('admin_cities_list') }}">Cities</a>
                        </li>
                        <li {% if 'genres' in app.request.get('_route') %}class="active"{% endif %}>
                            <a href="{{ path('admin_genres_list') }}">Genres</a>
                        </li>
                        <li {% if 'notifications' in app.request.get('_route') %}class="active"{% endif %}>
                            <a href="{{ path('admin_notifications_list') }}">Notifications</a>
                        </li>
                        <li {% if 'shares' in app.request.get('_route') %}class="active"{% endif %}>
                            <a href="{{ path('admin_shares_list') }}">Shares</a>
                        </li>
                        <li {% if 'customers' in app.request.get('_route') %}class="active"{% endif %}>
                            <a href="{{ path('admin_customers_list') }}">Customers</a>
                        </li>
                        <li {% if 'users' in app.request.get('_route') %}class="active"{% endif %}>
                            <a href="{{ path('admin_users_list') }}">Users</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('fos_user_security_logout') }}">Logout <span class="fa fa-sign-out"></span></a></li>
            </ul>
        </div>
    </div>
</nav>