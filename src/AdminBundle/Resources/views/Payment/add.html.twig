{% extends 'AdminBundle:layouts:main.html.twig' %}

{% block content %}
    <a class="btn btn-danger pull-right" href="{{ app.request.headers.get('referer') ? app.request.headers.get('referer') : path('admin_rooms_schedule', {slug: app.request.get('slug')}) }}">Cancel</a>
    <h1 class="page-header">Add Payment</h1>
    {{ include('AdminBundle:Payment:_form.html.twig') }}
{% endblock %}