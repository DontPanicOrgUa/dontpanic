{% extends 'AdminBundle:layouts:main.html.twig' %}

{% block content %}
    <a class="btn btn-danger pull-right" href="{{ path('admin_rooms_list') }}">Cancel</a>
    <h1 class="page-header">Edit Room</h1>
    {{ include('AdminBundle:Room:_form.html.twig') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $("#admin_bundle_city_form_type_roomManagers").chosen();
        $("#admin_bundle_city_form_type_timezone").chosen({
            search_contains: true
        });
    </script>
{% endblock %}